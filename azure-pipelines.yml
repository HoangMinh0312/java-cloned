# azure-pipelines.yml
trigger:
- main

pool:
  name: Default

jobs:
- job: ContainerJob
  displayName: 'Run in Container'
  container:
    image: node:18
  steps:
  - script: node --version
    displayName: 'Check Node version'
  - script: echo "Hello from container!"
    displayName: 'Hello'